<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.checkin.mapper.LoginHistoryMapper">
    <insert id="createHistoryLogin">
        insert into admin_login_history (employee_code, date, login_time) values(#{employeeCode}, now(), now())
    </insert>
    <select id="countHistoryLogin" resultType="java.lang.Integer">
        select count(*) from admin_login_history where employee_code = #{employeeCode} and date=CURRENT_DATE
    </select>
</mapper>